package com.shares.rest.api.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.shares.rest.api.entity.CfgSchool;

public interface CfgSchoolRepository extends JpaRepository<CfgSchool, byte[]> {}
